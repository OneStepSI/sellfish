<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eldi.sellfish">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:noHistory="true"
            android:theme="@style/fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/fullscreen" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/fullscreen" />
        <activity
            android:name=".MenuAwalActivity"
            android:theme="@style/fullscreen" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".PenjualActivity" />
        <activity android:name=".PembeliActivity" />
        <activity android:name=".SupirActivity" />
        <activity
            android:name=".about"
            android:theme="@style/fullscreen"></activity>
        <activity android:name=".profil_user"></activity>
    </application>

</manifest>